"""arc-sub-agent-container — a container-orchestration sub-agent.

  arc_sub_agent_container/
    spec.py              — the SubAgentSpec + the entry-point `build` function
    prompts/system.md    — the container-expert system prompt

Requires the `cos` MCP server for its container_*/network_* tools. See README.
"""
__version__ = "0.1.0"
